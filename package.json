{
  "name": "gtm-ops-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Internal GTM ops automation: inbound deal -> enrich -> score -> route across sales/finance/legal, with loud failure handling and observability.",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "demo": "tsx src/cli.ts demo",
    "run": "tsx src/cli.ts run",
    "serve": "tsx src/cli.ts serve",
    "doctor": "tsx src/cli.ts doctor",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
